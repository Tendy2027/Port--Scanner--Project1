# Port--Scanner--Project1
Built a multi-threaded TCP port scanner in Python to check open/closed/timed-out ports on a host. Uses socket programming for connections, threading for concurrency, argparse for host/range options, logging for results, and exception handling.
